class CachePolicy {
  static const learningSubjects = Duration(hours: 12);
  static const learningChapters = Duration(hours: 12);
  static const examSubjects = Duration(hours: 12);
  static const examPapers = Duration(hours: 12);
  static const chapterResources = Duration(hours: 12);
  static const chapterQuizzes = Duration(hours: 12);
  static const hubLists = Duration(minutes: 10);
  static const feedTimeline = Duration(minutes: 10);
  static const hubBadge = Duration(seconds: 45);
  static const marketplaceSearch = Duration(minutes: 10);
  static const orientationQuestionnaire = Duration(hours: 6);
  static const appConfig = Duration(minutes: 20);
}

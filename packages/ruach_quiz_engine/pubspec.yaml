name: ruach_quiz_engine
description: "Moteur de quiz RuachEdu — parsing QDL, moteur de session, renderers."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_math_fork: ^0.7.2
  flutter_highlight: ^0.7.0
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  flutter_markdown_plus: ^1.0.6
  phosphor_flutter: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
